#pragma once

#include <string_view>

namespace gentest::codegen::artifact_manifest {

inline constexpr std::string_view kSchema = "gentest.artifact_manifest.v1";

} // namespace gentest::codegen::artifact_manifest
