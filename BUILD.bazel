load('//build_defs:gentest.bzl', 'gentest_suite')

cc_library(
    name = 'gentest_runtime',
    srcs = ['src/runner_impl.cpp'],
    hdrs = glob(['include/gentest/*.h', 'include/gentest/*.hpp', 'include/gentest/*.inl']),
    copts = ['-std=c++23', '-DFMT_HEADER_ONLY', '-Iinclude', '-Itests'],
    includes = ['include'],
    visibility = ['//visibility:public'],
)

gentest_suite('unit')
gentest_suite('integration')
gentest_suite('fixtures')
gentest_suite('skiponly')

