workspace(name = "gentest")

# Optional: bazel_skylib (used by load, harmless if not present via MODULE)
load("@bazel_tools//tools/build_defs/repo:http.bzl", "http_archive")

# Keep empty for now; system libs are used.

